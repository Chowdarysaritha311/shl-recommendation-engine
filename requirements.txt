fastapi==0.110.0
uvicorn==0.29.0
streamlit==1.32.0
requests==2.31.0
beautifulsoup4==4.12.3
lxml==4.9.3       # Downgraded to avoid libxml2/libxslt build issues
pandas==2.1.1     # Optional: add if your recommender uses pandas
numpy==1.26.2     # Optional: add if your recommender uses numpy
scikit-learn==1.3.1  # Optional: add if your recommender uses ML
PyYAML==6.0       # Optional: if render.yaml is loaded
